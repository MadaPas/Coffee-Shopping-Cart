module.exports = {
    database:
      "mongodb+srv://<username>:<password>@<cluster-name>.tl0uu.mongodb.net/<database-name>?retryWrites=true&w=majority"
      // Replace <username> with the your username
      // Replace <password> with the password for the <username> user. 
      // Replace <database-name> with the name of the database that connections will use by default. 
      // Ensure any option params are URL encoded.
  };